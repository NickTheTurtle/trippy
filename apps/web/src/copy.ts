/**
 * The copy now lives in @trippy/copy so the mobile client can share the exact
 * same wording. This re-export keeps every existing `~/copy` import working.
 */
export { copy } from '@trippy/copy';
